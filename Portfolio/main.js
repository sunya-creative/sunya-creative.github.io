// Refined Image Metadata (Inlined to avoid CORS)
const imageMetadata = {
    "ap_beauty_wordmark": {
        "2": {
            "type": "single",
            "photos": [
                "asset/ap_beauty_wordmark/main.jpg"
            ],
            "ars": [1.7778]
        }
    },
    "laneige_si": {
        "2": {
            "type": "single",
            "photos": [
                "asset/laneige_si/page_2_img_2.jpg"
            ],
            "ars": [1.5015]
        },
        "3": {
            "type": "single",
            "photos": [
                "asset/laneige_si/page_3_img_2.jpg"
            ],
            "ars": [1.5015]
        },
        "4": {
            "type": "single",
            "photos": [
                "asset/laneige_si/page_4_img_2.jpg"
            ],
            "ars": [1.4993]
        },
        "5": {
            "type": "split-2-1",
            "photos": [
                "asset/laneige_si/page_5_img_3.jpg",
                "asset/laneige_si/page_5_img_4.jpg",
                "asset/laneige_si/page_5_img_2_cropped.jpg"
            ],
            "ars": [1.212, 1.2143, 0.768]
        },
        "6": {
            "type": "single",
            "photos": [
                "asset/laneige_si/la_si_06_highres.png"
            ],
            "ars": [1.6261]
        },
        "7": {
            "type": "grid",
            "photos": [
                "asset/laneige_si/page_7_img_4.jpg",
                "asset/laneige_si/page_7_img_3.jpg",
                "asset/laneige_si/page_7_img_2.jpg",
                "asset/laneige_si/page_7_img_5.jpg",
                "asset/laneige_si/page_7_img_6.jpg",
                "asset/laneige_si/page_7_img_7.jpg"
            ],
            "ars": [0.7995, 0.7995, 0.8, 1.4982, 1.3325, 0.865]
        },
        "8": {
            "type": "grid",
            "photos": [
                "asset/laneige_si/la_si_08_highres_1.png",
                "asset/laneige_si/la_si_08_highres_2.png"
            ],
            "ars": [1.7874, 1.7616]
        },
        "9": {
            "type": "grid",
            "photos": [
                "asset/laneige_si/page_8_img_6.jpg",
                "asset/laneige_si/page_8_img_7.jpg"
            ],
            "ars": [1.221, 1.9562]
        },
        "10": {
            "type": "single",
            "photos": [
                "asset/laneige_si/la_si_10.jpg"
            ],
            "ars": [1.6986]
        }
    },
    "ap_beauty": {
        "3": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/ap_01_highres.png"
            ],
            "ars": [1.6283]
        },
        "4": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/page_4_img_2.jpg"
            ],
            "ars": [1.3333]
        },
        "5": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/ap_05_highres.png"
            ],
            "ars": [1.6276]
        },
        "6": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/page_6_img_2.jpg"
            ],
            "ars": [1.4993]
        },
        "7": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_7_img_2.jpg",
                "asset/ap_beauty/page_7_img_4.jpg"
            ],
            "ars": [0.7147, 0.7147]
        },
        "8": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_8_img_2.jpg",
                "asset/ap_beauty/ap_08_highres.png"
            ],
            "ars": [0.7136, 1.6450]
        },
        "9": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_14_img_2.jpg",
                "asset/ap_beauty/ap_14_highres.png"
            ],
            "ars": [0.75, 1.3366]
        },
        "10": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/ap_17_highres_1.png",
                "asset/ap_beauty/ap_17_highres_2.jpg"
            ],
            "ars": [0.6658, 0.75]
        },
        "11": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/ap_shinbon_3.jpg",
                "asset/ap_beauty/ap_shinbon_5.jpg"
            ],
            "ars": [0.75, 1.3333]
        },
        "12": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/ap_shinbon_06.jpg",
                "asset/ap_beauty/ap_shinbon_26.jpg"
            ],
            "ars": [0.75, 0.667]
        },
        "13": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_9_img_2.jpg",
                "asset/ap_beauty/page_9_img_3.jpg"
            ],
            "ars": [1.3351, 1.3325]
        },
        "14": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_10_img_2.jpg",
                "asset/ap_beauty/page_10_img_3.jpg",
                "asset/ap_beauty/ap_10_highres.jpg",
                "asset/ap_beauty/page_10_img_5.jpg"
            ],
            "ars": [1.4294, 1.4988, 1.3333, 1.4974]
        },
        "15": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/ap_11_highres.png"
            ],
            "ars": [1.6281]
        },
        "16": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/page_12_img_2.jpg"
            ],
            "ars": [1.3342]
        },
        "17": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/page_13_img_2.jpg"
            ],
            "ars": [1.5006]
        },
        "18": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_15_img_2.jpg",
                "asset/ap_beauty/ap_15_highres.png"
            ],
            "ars": [1.3333, 1.3348]
        },
        "19": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/page_16_img_2.jpg"
            ],
            "ars": [1.3337]
        },
        "20": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/ap_sogo_4929.jpg"
            ],
            "ars": [1.778]
        },
        "21": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/ap_sogo_08.jpg",
                "asset/ap_beauty/ap_sogo_09.jpg"
            ],
            "ars": [1.0894, 1.5]
        },
        "22": {
            "type": "single",
            "photos": [
                "asset/ap_beauty/page_19_img_2.jpg"
            ],
            "ars": [1.3344]
        },
        "23": {
            "type": "grid",
            "photos": [
                "asset/ap_beauty/page_20_img_2.jpg",
                "asset/ap_beauty/page_20_img_3.jpg",
                "asset/ap_beauty/page_20_img_5.jpg",
                "asset/ap_beauty/page_20_img_6.jpg"
            ],
            "ars": [1.4986, 0.7496, 1.3333, 1.3333]
        }
    },
    "lirikos": {
        "2": {
            "type": "grid",
            "photos": [
                "asset/lirikos/liri_01.png",
                "asset/lirikos/liri_02.png"
            ],
            "ars": [2.4690, 0.9781],
            "gap": 58
        },
        "3": {
            "type": "grid",
            "photos": [
                "asset/lirikos/liri_03.png",
                "asset/lirikos/liri_04.png"
            ],
            "ars": [1.4558, 1.7109],
            "gap": 58
        },
        "4": {
            "type": "grid",
            "photos": [
                "asset/lirikos/liri_05.png",
                "asset/lirikos/liri_06.png",
                "asset/lirikos/liri_07.png"
            ],
            "ars": [2.2220, 2.2399, 6.0452],
            "rows": [[0, 1], [2]]
        }
    },
    "mise_en_scene": {
        "2": {
            "type": "grid",
            "photos": [
                "asset/mise_en_scene/mi_01.png",
                "asset/mise_en_scene/mi_02.png"
            ],
            "ars": [2.9075, 1.3044],
            "gap": 58
        },
        "3": {
            "type": "grid",
            "photos": [
                "asset/mise_en_scene/mi_03.png",
                "asset/mise_en_scene/mi_04.png"
            ],
            "ars": [1.2575, 0.5610],
            "gap": 58
        },
        "4": {
            "type": "single",
            "photos": [
                "asset/mise_en_scene/mi_05.png"
            ],
            "ars": [2.3343]
        }
    },
    "promotion_mbs": {
        "2": {
            "type": "single",
            "photos": [
                "asset/promotion_mbs/la_se_03.jpg"
            ],
            "ars": [2.3048]
        },
        "3": {
            "type": "single",
            "photos": [
                "asset/promotion_mbs/la_se_dubai_gondola.png"
            ],
            "ars": [1.6333]
        },
        "4": {
            "type": "split-1-3",
            "photos": [
                "asset/promotion_mbs/la_se_kingscross_left.png",
                "asset/promotion_mbs/la_se_kingscross_right_1.png",
                "asset/promotion_mbs/la_se_kingscross_right_2.png",
                "asset/promotion_mbs/la_se_kingscross_right_3.png"
            ],
            "ars": [1.1459, 1.5069, 1.5192, 1.3375]
        },
        "5": {
            "type": "split-1-subgrid",
            "photos": [
                "asset/promotion_mbs/la_se_westfield_left.png",
                "asset/promotion_mbs/la_se_westfield_right_top.png",
                "asset/promotion_mbs/la_se_westfield_right_bottom_1.png",
                "asset/promotion_mbs/la_se_westfield_right_bottom_2.png"
            ],
            "ars": [0.8906, 1.4932, 0.7077, 0.7316]
        },
        "6": {
            "type": "single",
            "photos": [
                "asset/promotion_mbs/la_se.png"
            ],
            "ars": [1.4181]
        },
        "7": {
            "type": "split-3col-leftstacked",
            "photos": [
                "asset/promotion_mbs/la_se_mbs_left_top.png",
                "asset/promotion_mbs/la_se_mbs_left_bottom.png",
                "asset/promotion_mbs/la_se_mbs_center.png",
                "asset/promotion_mbs/la_se_mbs_right.png"
            ],
            "ars": [1.4150, 1.4286, 0.4294, 0.8074]
        },
        "8": {
            "type": "grid",
            "photos": [
                "asset/promotion_mbs/page_2_img_2.jpg",
                "asset/promotion_mbs/page_2_img_3.jpg"
            ],
            "ars": [0.6671, 0.6667]
        },
        "9": {
            "type": "grid",
            "photos": [
                "asset/promotion_mbs/pm_09_highres.jpg",
                "asset/promotion_mbs/page_3_img_3.jpg"
            ],
            "ars": [1.0, 0.606]
        }
    },
    "aritaum_shanghai": {
        "2": {
            "type": "single",
            "photos": [
                "asset/aritaum_shanghai/ar_02_highres.png"
            ],
            "ars": [1.6298]
        },
        "3": {
            "type": "grid",
            "photos": [
                "asset/aritaum_shanghai/ar_03_highres_1.png",
                "asset/aritaum_shanghai/ar_03_highres_2.png"
            ],
            "ars": [0.6560, 1.5142]
        },
        "4": {
            "type": "single",
            "photos": [
                "asset/aritaum_shanghai/Portfolio_full_20260104--.jpeg"
            ],
            "ars": [1.5]
        },
        "5": {
            "type": "grid",
            "photos": [
                "asset/aritaum_shanghai/ar_05_highres_1.png",
                "asset/aritaum_shanghai/ar_05_highres_2.png",
                "asset/aritaum_shanghai/ar_05_highres_3.png",
                "asset/aritaum_shanghai/ar_05_highres_4.png"
            ],
            "ars": [1.5789, 1.5124, 1.5661, 1.5149]
        },
        "6": {
            "type": "split-1-2",
            "photos": [
                "asset/aritaum_shanghai/page_3_img_2.jpg",
                "asset/aritaum_shanghai/page_3_img_3.jpg",
                "asset/aritaum_shanghai/page_3_img_4.jpg"
            ],
            "ars": [1.4648, 1.456, 1.4501]
        },
        "7": {
            "type": "single",
            "photos": [
                "asset/aritaum_shanghai/ar_06_highres.jpg"
            ],
            "ars": [1.5469]
        },
        "8": {
            "type": "vertical",
            "photos": [
                "asset/aritaum_shanghai/ari_01.png",
                "asset/aritaum_shanghai/ari_02.png"
            ],
            "ars": [2.8058, 2.2331]
        },
        "9": {
            "type": "single",
            "photos": [
                "asset/aritaum_shanghai/ar_07_highres.jpg"
            ],
            "ars": [1.4998]
        }
    },
    "mamonde": {
        "2": {
            "type": "single",
            "photos": [
                "asset/mamonde/ma_02_highres.png"
            ],
            "ars": [1.625]
        },
        "3": {
            "type": "single",
            "photos": [
                "asset/mamonde/ma_03_highres.png"
            ],
            "ars": [1.6238]
        },
        "4": {
            "type": "grid",
            "photos": [
                "asset/mamonde/page_4_img_2.jpg",
                "asset/mamonde/page_4_img_3.jpg",
                "asset/mamonde/ma_04_highres.png"
            ],
            "ars": [0.6665, 0.6672, 0.7129]
        },
        "5": {
            "type": "grid",
            "photos": [
                "asset/mamonde/page_5_img_2.jpg",
                "asset/mamonde/page_5_img_3.jpg"
            ],
            "ars": [0.7856, 0.6671]
        },
        "6": {
            "type": "single",
            "photos": [
                "asset/mamonde/page_6_img_2.jpg"
            ],
            "ars": [1.4985]
        },
        "7": {
            "type": "grid",
            "photos": [
                "asset/mamonde/page_7_img_2.jpg",
                "asset/mamonde/page_7_img_3.jpg"
            ],
            "ars": [0.7504, 0.6662]
        }
    },
    "laneige_flagship": {
        "2": {
            "type": "single",
            "photos": [
                "asset/laneige_flagship/page_2_img_2.jpg"
            ],
            "ars": [1.6254]
        },
        "3": {
            "type": "single",
            "photos": [
                "asset/laneige_flagship/page_4_img_2.jpg"
            ],
            "ars": [1.3843]
        },
        "4": {
            "type": "grid",
            "photos": [
                "asset/laneige_flagship/page_5_img_5.jpg",
                "asset/laneige_flagship/page_5_img_3.jpg",
                "asset/laneige_flagship/page_5_img_4.jpg",
                "asset/laneige_flagship/page_5_img_2.jpg"
            ],
            "ars": [1.5016, 1.5031, 1.6206, 1.5338]
        },
        "5": {
            "type": "single",
            "photos": [
                "asset/laneige_flagship/page_6_img_2.jpg"
            ],
            "ars": [2.252]
        },
        "6": {
            "type": "grid",
            "photos": [
                "asset/laneige_flagship/page_7_img_2.jpg",
                "asset/laneige_flagship/page_7_img_3.jpg"
            ],
            "ars": [1.5791, 1.5217]
        },
        "7": {
            "type": "single",
            "photos": [
                "asset/laneige_flagship/la_fla_paper.png"
            ],
            "ars": [2.7275]
        },
        "8": {
            "type": "grid",
            "photos": [
                "asset/laneige_flagship/page_9_img_4.jpg",
                "asset/laneige_flagship/page_9_img_2.jpg",
                "asset/laneige_flagship/page_9_img_3.jpg"
            ],
            "ars": [0.6667, 0.7289, 0.7289]
        }
    }
};

// 1. Detailed Project Database (Extracted & Decoded from original Portfolio PDF)
const projectData = {
    ap_beauty_wordmark: {
        cat: "BRAND IDENTITY",
        title: "AP BEAUTY WORDMARK",
        period: "2025.11 - 2026.05",
        role: "AP BEAUTY 워드마크 및 브랜드 로고 시스템 개발",
        desc: "브랜드 인지도를 높이고 다양한 접점에서 일관되게 활용할 수 있는 로고 시스템을 구축",
        tasks: [
            "브랜드 접점 확장과 실제 사용 환경을 고려한 워드마크 개발 방향 수립",
            "기존 AP 모노그램과 AP BEAUTY 브랜드명을 연결하는 로고 구조 개발",
            "리테일·디지털·인쇄 등 여러 환경에 일관되게 적용할 수 있는 브랜드 아이덴티티 시스템 구축",
            "모노그램과 워드마크가 상호 보완적으로 활용되는 AP BEAUTY 로고 시스템 구축"
        ],
        contribution: [
            { label: "아이덴티티 디자인 기획", val: 100 },
            { label: "로고 시스템 구축", val: 100 }
        ],
        accentColor: "#E5C158",
        slidesCount: 2
    },
    ap_beauty: {
        cat: "VMD (DS, MALL, POP-UP)",
        title: "AP BEAUTY REBRANDING",
        period: "2023.07 - Present",
        role: "AP브랜드 리브랜딩 신규 S.I. 백화점, 몰타입 VMD",
        desc: "2023년부터 AP BEAUTY 리브랜딩 프로젝트에 참여하여, 브랜드의 기술력과 고효능 이미지를 전달하는 신규 공간 아이덴티티와 VMD 디자인을 수행했습니다. 혁신적 기술, 독자 개발 성분, 압도적 효능이라는 브랜드의 핵심 가치를 바탕으로 ‘미래피부연구소’를 공간 콘셉트로 설정하고, 다양한 매장 환경에서도 일관되게 적용할 수 있는 디자인 기준을 정립했습니다. 경계와 모서리를 최소화한 곡선형 구조와 화이트 톤, 금속 소재를 활용해 이성적이고 정교한 이미지를 강조했으며, IMC의 클리니컬 시술 이미지를 입체적인 오브제와 디스플레이로 재해석해 과학적 전문성과 시각적 차별성을 강화했습니다. 주요 제품 라인에는 정제된 모듈형 진열 구조를 적용하여 공간과 제품이 자연스럽게 연결되면서도 제품이 중심적으로 인지되도록 구성했습니다. 개발된 공간 아이덴티티를 백화점, 몰, 팝업 등 국내외 매장에 확장하고, 일관된 브랜드 경험을 구현할 수 있는 글로벌 VMD 가이드를 구축했습니다.",
        tasks: [
            "신규 럭셔리 브랜드 런칭을 위한 공간 디자인 기획",
            "브랜드 핵심 가치를 반영한 공간 디자인 기획",
            "클리니컬 시술 이미지를 강조하고 시각적 차별화 강화",
            "IMC 시술 이미지를 입체적으로 시각화",
            "브랜드를 대표할 수 있는 오브제 디자인",
            "글로벌 매장 확산을 위한 브랜드 아이덴티티 확립"
        ],
        contribution: [
            { label: "디자인 기획 및 S.I. 정립", val: 50 },
            { label: "VMD 디자인 개발", val: 70 },
            { label: "글로벌 확산 및 가이드 정립", val: 80 }
        ],
        accentColor: "#E5C158", // Luxury Gold
        slidesCount: 23,
        designPoints: {
            7: "클리니컬 시술 이미지를 입체적인 VMD로 재해석하여 브랜드의 과학적 전문성과 시각적 차별성을 강화했습니다.",
            8: "경계와 모서리를 절제한 유기적 구조에 화이트 톤과 메탈 소재를 적용하여 이성적이고 정제된 이미지를 구현했습니다.",
            9: "안티에이징 효능을 시각화하고, 제품에 담긴 블랙 다이아몬드의 빛과 반짝임이 극대화되도록 디테일을 설계했습니다.",
            10: "브랜드를 대표하는 오브제를 디자인하고 섬세한 금속 포인트를 적용하여 차별화된 무드를 조성했습니다.",
            12: "리브랜딩 론칭 2년 후 집기와 VMD를 하나의 시스템으로 재정비하고,<br>기존의 클리니컬한 디자인 디테일은 일관되게 유지하며 신규 글로리파이어를 더해 IMC의 주목도를 강화했습니다.",
            21: "현지 고객과 공간 특성을 반영한 시너지 비주얼을 개발하고, 컨셉을 직관적으로 전달하는 팝업 VMD를 완성했습니다."
        },
        stores: [
            "롯데백화점 본점, 잠실점 (2024년 1월 오픈)",
            "현대백화점 본점, 목동점, 무역점, 판교점 (2024년 1월 오픈)",
            "신세계 백화점 본점, 강남점, 경기점 (2024년 1월 오픈)",
            "갤러리아 압구정점 (2024년 1월 오픈)",
            "홍콩 AP SUITE 스파 (2024년 1월 오픈)",
            "홍콩 하버시티 Mall (2024년 3월 오픈)",
            "홍콩 IFC Mall (2024년 3월 오픈)",
            "홍콩 타임스퀘어 (2024년 3월 오픈)",
            "홍콩 하버시티 팝업 (1년/2024년 3월 오픈)",
            "홍콩 타임스퀘어 팝업 (2주/2024년 4월 오픈)",
            "중국 상해 지우광점 (2024년 7월 오픈)",
            "미국 버그도프굿맨 백화점 (2025년 5월 오픈)",
            "홍콩 엘리먼츠몰 팝업 (1년/2025년 7월 오픈)",
            "홍콩 타임스퀘어 팝업 (2주/2025년 10월 오픈)",
            "미국 블루밍데일즈 백화점 (2025년 11월 오픈)",
            "신세계 백화점 본점 MD개편 (2026년 1월 오픈)",
            "홍콩 소고백화점 팝업 (2주/2026년 4월 오픈)"
        ]
    },
    laneige_si: {
        cat: "VMD (DS, MALL) / PACKAGING / VISUAL",
        title: "LANEIGE New S.I.",
        period: "2021.01 - 2023.06",
        role: "라네즈 신규 S.I. 백화점, 몰타입 VMD",
        desc: "라네즈 신규 S.I. 프로젝트에 참여하여 글로벌 공간 아이덴티티를 구축하고, 제품 진열과 고객 경험을 개선하는 VMD 디자인을 수행했습니다. 다양한 제품군과 국가별 운영 환경을 고려해 정보 전달이 명확하면서도 현지 운영이 용이한 확장형 진열 시스템을 개발했습니다. 메이크업 집기는 제품 특성에 따라 카테고리를 구분하고 구조와 비주얼 그래픽을 개선해 직관적인 제품 탐색을 유도했으며, 이후 교체가 쉬운 모듈형 구조로 발전시켜 유지관리 효율을 높였습니다. 아트 콘텐츠 존은 신진 작가의 작품과 제품이 자연스럽게 조화를 이루면서도 콘텐츠 변경에 유연하게 대응할 수 있도록 설계했습니다. 패키지에는 친환경 소재를 적용하고 구조와 규격을 최적화해 자재 사용을 줄였습니다. 이를 통해 국내외 매장에 일관된 브랜드 경험을 구현하고, 운영 효율과 지속가능성을 고려한 글로벌 VMD 시스템을 구축했습니다.",
        tasks: [
            "신규 S.I. 개발을 통한 라네즈의 새로운 공간 이미지 구축",
            "제품 구성과 운영 환경을 고려한 진열 디자인 최적화",
            "제품 정보 전달과 진열 효율을 높인 메이크업 집기 구조 개발",
            "페이스 메이크업 전문성을 강화하기 위한 신규 비주얼 기획",
            "새로운 고객 경험을 제공하는 아트 콘텐츠 존 VMD 기획",
            "국내 신진 작가와 협업한 아트 오브제 및 집기 디자인 개발",
            "고객 동선을 고려한 베네핏 중심 VMD 개편",
            "지속가능한 매장 패키지 디자인",
            "글로벌 확산을 위한 VMD 디자인 및 운영 가이드 정립"
        ],
        contribution: [
            { label: "디자인 기획 및 S.I. 정립", val: 50 },
            { label: "VMD 디자인 개발", val: 50 },
            { label: "글로벌 확산 및 가이드 정립", val: 80 },
            { label: "그래픽 및 패키지 디자인", val: 100 },
            { label: "비주얼 디렉팅", val: 30 }
        ],
        accentColor: "#00A3FF", // Laneige Blue
        slidesCount: 10,
        designPoints: {
            5: "베이스, 매트, 글로우 등 표현 효과에 따라 제품을 체계적으로 조닝하고, 제품별 정보 전달을 강화해 고객의 탐색 편의성과 주목도를 높였습니다.<br>제품의 제형과 사용 효과를 감각적인 비주얼로 구현하여 라네즈의 페이스 메이크업 전문성을 효과적으로 전달했습니다.<br>TCR 과정에서 집기 구조를 교체가 용이한 모듈형 유닛으로 개선하여 글로벌 매장의 운영과 유지관리 효율을 높였습니다.",
            7: "작가의 작품과 라네즈 제품이 하나의 장면처럼 자연스럽게 어우러지면서도, 작품 교체에 유연하게 대응할 수 있는 전시 구조를 개발했습니다.<br>국내 신진 작가와의 협업을 통해 라네즈의 창의적인 브랜드 이미지를 전달하는 차별화된 아트 집기를 완성했습니다.",
            8: "제품의 핵심 베네핏을 중심으로 진열 체계를 재구성하고, 카테고리별 조닝이 명확하게 인지되도록 공간 구조와 VMD를 연계했습니다.",
            9: "글로벌 현지 팀이 매장 환경에 맞춰 세트와 제품을 쉽고 일관되게 진열할 수 있도록 구성 요소와 운영 방식을 표준화했습니다.",
            10: "재생지와 FSC 지류를 사용하여 친환경적인 요소를 반영하고, 가장 효율적인 사이즈를 계산하여 지류 사용을 최소화하였습니다.<br>확대된 로고와 리본 컬러 플레이를 활용해 원거리 주목도를 높이고, 라네즈 특유의 생동감 있는 브랜드 이미지를 강조했습니다."
        },
        stores: [
            "대만 Chungyo 백화점 (22년 1월 오픈)",
            "중국 Xian KaiYuan 백화점 (22년 6월 오픈)",
            "홍콩 Metroplaza Mall (22년 7월 오픈)",
            "홍콩 Sogo 백화점 (22년 8월 오픈)",
            "중국 Haikou 면세점 (22년 10월 오픈)",
            "중국 Nanjing Jongang 백화점 (22년 11월 오픈)",
            "태국 Emporium 백화점 (22년 11월 오픈)",
            "중국 Lanzhou (22년 12월 오픈)",
            "말레이시아 Bukit Jalil (23년 7월 오픈)"
        ]
    },
    laneige_flagship: {
        cat: "VMD (FLAGSHIP) / GRAPHIC",
        title: "LANEIGE FLAGSHIP STORE",
        period: "2020.01 - 2020.12",
        role: "라네즈 명동 플래그십스토어 VMD",
        desc: "라네즈 명동 플래그십 스토어 리뉴얼 프로젝트에서 브랜드 정체성을 강화하고 고객의 제품 탐색과 체험 경험을 개선하는 VMD 디자인을 수행했습니다. 기존의 라인별 제품 진열 방식이 아닌 피부 유형과 고민을 기준으로 제품을 재구성하고, 직원의 도움 없이도 제품의 기능과 차이를 직관적으로 이해할 수 있는 네비게이션 시스템을 개발했습니다. 입구의 메인 아일랜드에는 라네즈의 5대 핵심 제품을 Water Scene 오브제와 함께 구성하여 수분 기능성 브랜드의 이미지를 시각적으로 강화했습니다. 또한 제품의 제형, 향, 질감을 직접 경험할 수 있도록 체험 요소를 확장하여 브랜드 철학과 제품 효능을 오감으로 전달했습니다. 온라인 쇼핑에 익숙한 고객의 행동 패턴을 반영하여 매장 곳곳에 전제품 주문서를 배치하고, 제품을 직접 들고 이동하거나 쇼핑 바구니를 사용하지 않아도 편리하게 구매할 수 있는 환경을 구축했습니다. 이를 통해 체험과 구매 기능을 분리하여 고객이 제품 경험에 더욱 집중할 수 있도록 설계했습니다. 또한 제품을 체험하며 머물 수 있는 휴식 공간을 마련해 매장을 단순한 판매 공간이 아닌 브랜드를 온전히 경험하는 공간으로 확장했습니다. 결과적으로 제품 탐색, 체험, 구매, 휴식이 자연스럽게 연결되는 통합적인 플래그십 스토어 고객 경험을 구축했습니다.",
        tasks: [
            "고객의 취향과 쇼핑 행태를 분석한 VMD 디자인",
            "제품 체험과 쇼핑 편의성을 고려한 고객 동선 설계",
            "피부 유형과 고민별로 세분화한 제품 네비게이션 시스템 개발",
            "온라인 쇼핑 방식을 적용한 전제품 주문 시스템 기획",
            "Water Scene을 활용한 브랜드 대표 VMD 오브제 개발",
            "지속가능성을 반영한 매장 기프트 패키지 디자인"
        ],
        contribution: [
            { label: "디자인 기획 및 S.I. 정립", val: 70 },
            { label: "VMD 디자인 개발", val: 80 },
            { label: "그래픽 및 패키지 디자인", val: 100 }
        ],
        accentColor: "#00C2FF", // Ocean Cyan
        slidesCount: 8,
        designPoints: {
            4: "Water Scene을 모티브로 한 VMD 오브제를 개발하여 라네즈의 수분 기능성과 브랜드 철학을 직관적으로 시각화했습니다.",
            6: "피부 유형과 고민을 기준으로 제품을 세분화하고, 온라인 쇼핑의 직관적인 네비게이션 방식을 리테일 공간에 적용했습니다.",
            7: "제품 체험에 집중할 수 있도록 매장에 전제품 주문서를 배치하여 쇼핑바구니 없이도 편리하게 제품을 구매할 수 있도록 설계했습니다."
        }
    },
    aritaum_shanghai: {
        cat: "VMD (FLAGSHIP) / GRAPHIC / PACKAGING",
        title: "ARITAUM SHANGHAI",
        period: "2019.01 - 2020.01",
        role: "아리따움 상해 플래그십스토어 VMD",
        desc: "아리따움 상하이 플래그십 스토어 프로젝트에서 중국 밀레니얼 고객의 취향과 쇼핑 행태를 반영한 현지화 VMD 및 공간 디자인을 수행했습니다. 40여 개 브랜드를 하나의 매장에 통합하면서도 차별화된 멀티브랜드 경험을 제공하는 공간을 구축했습니다. 현지 고객의 소비 행태 분석을 통해 제품 구매뿐 아니라 체험, 체류, 콘텐츠 제작이 함께 이루어지는 흐름에 주목했습니다. 이를 바탕으로 ‘Tea Time Pleasure’ 콘셉트를 설정하고, 카페처럼 머물며 뷰티를 경험할 수 있는 공간을 기획했습니다. 매장에는 좌석형 테이블을 배치해 제품 테스트와 터치업이 가능하도록 했으며, 메뉴판 형식의 제품 정보 시스템으로 브랜드 정보를 직관적으로 전달했습니다. 매장 안쪽에는 각기 다른 테마의 부스를 조성해 고객이 자연스럽게 공간 깊숙이 이동하도록 유도했습니다. 포토제닉한 배경과 조명을 적용하여 고객과 KOL이 사진 촬영이나 SNS Live를 진행할 수 있는 콘텐츠 공간으로 활용할 수 있도록 설계했습니다. 중국 카메라 앱 환경에서의 시각적 효과를 고려해 파스텔 핑크를 주조색으로 선정하고, 카페 오브제를 적용해 공간 아이덴티티를 완성했습니다. 이를 통해 탐색, 체험, 휴식, 콘텐츠 제작이 자연스럽게 연결되는 고객 경험을 구축하고, 멀티브랜드 스토어 운영을 위한 공간 아이덴티티 및 S.I. 가이드를 정립했습니다.",
        tasks: [
            "중국 현지화를 위한 고객 분석 및 디자인 기획",
            "멀티브랜드스토어 구축 및 개발을 위한 신규 S.I. 가이드 작업",
            "밀레니얼 고객의 취향과 트렌드를 반영한 디자인 전개",
            "앉을 수 있는 매장으로 구성하여 체류시간 증대에 기여함",
            "고객 및 왕홍들이 SNS Live를 진행할 수 있도록 구성",
            "무드를 연출하기 위한 소품과 그래픽, 패키지 등을 개발"
        ],
        contribution: [
            { label: "디자인 기획 및 S.I. 정립", val: 80 },
            { label: "VMD 디자인 개발", val: 100 },
            { label: "현지 브랜딩 및 가이드 정립", val: 100 }
        ],
        accentColor: "#F87171", // Millennial Rose Pink
        slidesCount: 9,
        designPoints: {
            3: "중국 소비자가 사용하는 카메라 앱의 필터 환경에서 컬러 표현을 검토하고, 가장 부드럽고 매력적으로 보이는 파스텔 핑크를 공간의 주조색으로 선정했습니다.",
            4: "‘Tea Time Pleasure’ 콘셉트 아래 카페의 테이블웨어와 오브제를 VMD 요소로 재해석하여 편안하면서도 몽환적인 공간 무드를 완성했습니다.",
            6: "고객이 앉아서 자유롭게 제품을 테스트하고 터치업 서비스를 받을 수 있도록 구성하여 체류 시간을 늘리고 브랜드와의 접점을 확대하였습니다.",
            7: "매장 안쪽에 각기 다른 테마의 콘텐츠 부스를 배치하여 고객의 동선을 확장하고, 사진 촬영과 SNS Live가 가능한 공간을 구성했습니다.",
            8: "카페 메뉴판에서 착안한 제품 '메뉴'를 각 테이블에 배치하여 여러 브랜드의 제품 특징을 쉽고 직관적으로 탐색할 수 있도록 했습니다.",
            9: "카페 오브제와 화장품 형태를 핸드 드로잉 패턴으로 개발하여 공간 콘셉트를 확장하고, 일관된 그래픽과 패키지 디자인으로 연결했습니다."
        }
    },
    mamonde: {
        cat: "VMD (FLAGSHIP)",
        title: "MAMONDE FLAGSHIP STORE",
        period: "2016.12 - 2018.12",
        role: "마몽드 플래그십스토어, 백화점 VMD",
        desc: "마몽드 싱가폴 Ngee Ann City  플래그십 스토어 VMD 디자인은 브랜드의 핵심 가치인 꽃의 생명력과 아름다움을 매장 공간에 담아내는 데 중점을 두었습니다. 고객들이 브랜드의 철학을 직관적으로 경험할 수 있도록, 매장 내 각 조닝별로 압화, 드라이플라워, 조화 등을 활용해 꽃의 다양한 형태와 질감을 표현했습니다. 특히, 원료를 추출하는 과정을 오브제로 재해석하여 자연의 힘과 꽃의 에너지를 전달하는 데 집중했습니다. 이를 통해 매장 내부는 마몽드의 꽃을 압화 형태로 표현한 일러스트를 활용해 마몽드만의 ‘플라워 스튜디오’를 형상화한 공간으로 구성되었습니다. 또한, 메이크업 존에서는 마몽드의 대표 제품인 크레용 형태의 립 제품을 강조하기 위해 표구실, 화실 등을 연상시키는 집기와 오브제를 배치하여 차별화된 공간을 연출했습니다. 이러한 디자인은 브랜드의 정체성을 강화하고, 제품의 특성과 브랜드 아이덴티티를 직관적으로 전달하는 데 중요한 역할을 했습니다.",
        tasks: [
            "글로벌 플래그십스토어 오픈을 위한 신규 디자인 개발",
            "글로벌 확산을 위한 백화점 특화 신규 테스터 VMD 개발",
            "마몽드 대표 립제품 특징을 강조한 특화된 집기와 오브제 개발",
            "마몽드 국내 명동 부티크 월별 프로모션 디자인 진행"
        ],
        contribution: [
            { label: "VMD 디자인 개발", val: 50 },
            { label: "글로벌 확산 및 가이드 정립", val: 80 }
        ],
        accentColor: "#F43F5E", // Mamonde Flower Red
        slidesCount: 7,
        designPoints: {
            4: "압화, 드라이플라워, 조화 등 다양한 꽃 소재와 원료 추출 과정을 표현한 오브제를 통해 꽃의 생명력과 제품 철학을 입체적으로 시각화했습니다.",
            6: "대표 크레용 립 제품의 형태와 컬러를 강조하기 위해 표구실과 화실을 모티브로 한 차별화된 메이크업 존을 구성했습니다."
        }
    },
    promotion_mbs: {
        cat: "VMD (PROMOTION, MBS)",
        title: "PROMOTION & MBS",
        period: "프로젝트별 상이",
        role: "프로모션 및 MBS VMD",
        desc: "2016년 말부터 다양한 브랜드의 VMD 디자인을 담당하며, IMC 글로리파이어와 프로모션 디스플레이를 기획하고 실행했습니다. 브랜드 신규 런칭 행사, 시즌별 디스플레이, 포디움 디자인 등 다양한 프로젝트를 통해 브랜드에 신선함을 더하고 주목도를 높이는 데 주력했습니다. 각 브랜드에서 보여주고자 하는 메시지를 시각적으로 효과적으로 전달해 고객의 관심을 끌고 브랜드 경험을 극대화하는 데 집중했습니다.\nMBS 채널에 적용할 때에도, 각 국가의 유통사별 특화된 디자인을 진행하여 브랜드가 글로벌 시장에서도 경쟁력을 가질 수 있도록 했습니다. 각국의 시장성과 고객 성향을 고려해 매장에 적합한 디스플레이 전략을 수립하고, 현지 제작 사양에 맞춰 유연하게 디자인을 변형해 실행했습니다.",
        tasks: [
            "글로벌 리테일 및 프로모션 공간 디자인 기획 및 개발",
            "라네즈 중동 및 영국 진출을 위한 디자인 진행",
            "글로벌 시장 특성을 반영한 현지화 디자인 전개",
            "브랜드 컬러 및 디자인 시스템 개선",
            "홀리데이 프로모션 및 주요 캠페인 VMD"
        ],
        contribution: [
            { label: "디자인 기획", val: 90 },
            { label: "VMD 디자인 개발", val: 100 }
        ],
        accentColor: "#888888", // Neutral Slate
        slidesCount: 9,
        designPoints: {
            2: "라네즈의 중동 Sephora 론칭을 위해 두바이몰 윈도우를 디자인하고,<br>립 슬리핑 마스크의 향과 컬러를 감각적으로 시각화해 제품 주목도를 높였습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2023년 3월 ~ 4월  |  프로모션 기간 : 2023년 5월</span>",
            3: "라네즈 중동 세포라 진출을 위한 곤돌라 프로모션 디자인을 진행하였으며,<br>립 슬리핑 마스크와 워터 슬리핑 마스크를 균형 있게 구성하여 라네즈의 핵심 제품군이 직관적으로 인지되도록 디자인했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2023년 3월 ~ 4월  |  프로모션 기간 : 2023년 5월</span>",
            4: "라네즈 브랜드의 영국 현지 MBS 채널인 SPACE NK 입점을 위한 Kingscross점 브랜드존을 디자인하였으며,<br>립슬리핑 마스크와 모델 비주얼을 강조하여 브랜드 아이덴티티를 효과적으로 전달했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2023년 2월 ~ 4월  |  프로모션 기간 : 2023년 4월</span>",
            5: "라네즈 브랜드의 영국 현지 MBS 채널인 SPACE NK 입점을 위한 Westfield점 브랜드존을 디자인하였으며,<br>립슬리핑 마스크와 모델 비주얼을 강조하여 브랜드 아이덴티티를 효과적으로 전달했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2023년 2월 ~ 4월  |  프로모션 기간 : 2023년 4월</span>",
            6: "글로벌 MBS의 브랜드 인지도를 높이기 위해 라네즈의 컬러와 디자인 시스템을 재정비하고,<br>미국·호주·유럽·중동의 국가별, 채널별 요구에 맞춰 확장했습니다.",
            8: "AP BEAUTY의 2024 HOLIDAY PROMOTION은 아모레퍼시픽 본사 건축에서 영감을 받은 패턴 디자인으로 선물박스 슬리브를 디자인하였고, <br>AP BEAUTY의 3 Key Pillars를 상징하는 입체적인 삼각형 형태의 참 오너먼트를 디자인하여 쇼핑백 손잡이를 장식하였습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2024년 7월 ~ 10월  |  프로모션 기간 : 2024년 11월 ~ 2025년 1월</span>",
            9: "사옥의 정육면체 형태에서 영감을 받아 큐브 패턴을 매장 VMD 디자인에 적용하고, <br>블랙 배경에 골드와 실버 포인트로 고급스러움과 세련미를 강조하며 브랜드의 연결과 소통 가치를 시각적으로 표현했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2024년 7월 ~ 10월  |  프로모션 기간 : 2024년 11월 ~ 2025년 1월</span>"
        }
    },
    lirikos: {
        cat: "PACKAGING / GRAPHIC",
        title: "LIRIKOS",
        period: "2013.11 - 2014.10",
        role: "리리코스 제품 및 패키지 디자인, 브랜드북 기획",
        desc: "리리코스의 메이크업과 옴므 제품 라인을 중심으로 신규 용기와 패키지 디자인을 개발하고, 브랜드 헤리티지를 체계화하는 브랜드북을 기획했습니다. 메이크업 전 라인은 바닷가의 조약돌에서 영감을 받은 부드럽고 유기적인 형태로 용기 금형을 새롭게 개발했습니다. 패키지의 정보 레이아웃과 소재를 재정비하고 지류 사양을 업그레이드하여 제품 전반의 통일감과 고급감을 강화했습니다. 리리코스 옴므는 B.I.의 가독성을 개선하고, 고가 라인인 마린 미라클의 신규 용기와 패키지를 개발했습니다. 무게감 있는 용기와 절제된 형태를 적용해 남성 프리미엄 스킨케어의 전문적이고 고급스러운 이미지를 표현했습니다. 브랜드북은 리리코스의 해양 원료와 브랜드 스토리를 일관된 이미지로 전달할 수 있도록 콘텐츠 구성, 편집 디자인, 촬영 방향을 함께 기획했습니다. 이를 통해 제품 디자인부터 브랜드 콘텐츠까지 연결되는 통합적인 시각 아이덴티티를 구축했습니다.",
        tasks: [
            "리리코스 메이크업 전 라인의 신금형 및 패키지 디자인",
            "조약돌을 모티브로 한 유기적인 용기 형태 개발",
            "패키지 레이아웃 재정비 및 지류 업그레이드",
            "리리코스 옴므 B.I. 개발 및 로고 가독성 개선",
            "마린 미라클 고가 라인의 신규 용기 및 패키지 디자인",
            "브랜드 헤리티지 전달을 위한 브랜드북 구성 및 편집 디자인",
            "원료 중심의 이미지 매니지먼트 및 촬영 가이드 기획",
            "리리코스 전 제품 디자인 리프레임 TFT 참여"
        ],
        contribution: [
            { label: "그래픽 및 단상자 디자인", val: 100 },
            { label: "용기 패키지 디자인", val: 80 },
            { label: "비주얼 디렉팅", val: 30 }
        ],
        accentColor: "#14B8A6", // Lirikos Teal
        slidesCount: 4,
        designPoints: {
            2: "리리코스 메이크업 전라인의 신규 용기 및 지류 패키지 디자인을 진행하였으며, 조약돌 모티브를 반영한 용기 디자인을 완성했습니다.<br>패키지 정보 레이아웃과 지류 사양을 재정비하여 제품 간 통일감을 높이고 브랜드의 고급스러운 이미지를 강화했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2013년 11월 ~ 2014년 3월</span>",
            3: "리리코스 옴므 B.I.의 가독성을 개선하고, 무게감 있는 용기와 절제된 형태를 통해 마린 미라클 라인의 프리미엄 이미지를 구현했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2014년 4월 ~ 2014년 8월</span>",
            4: "해양 원료와 브랜드 스토리를 중심으로 브랜드북의 콘텐츠 구성, 편집 디자인 및 촬영 가이드를 기획하여 리리코스의 헤리티지를 일관되게 전달했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2014년 3월 ~ 2014년 10월</span>"
        }
    },
    mise_en_scene: {
        cat: "PACKAGING / B.I.",
        title: "MISE-EN-SCENE",
        period: "2014.11 - 2016.12",
        role: "미쟝센 로고타입 리디자인, 제품 및 패키지 디자인",
        desc: "미쟝센의 B.I. 정비와 주요 제품 라인의 용기 및 패키지 디자인을 담당하며, 브랜드 이미지와 매대 경쟁력을 강화하는 디자인을 수행했습니다. B.I. 전문 업체와 협업하여 기존 로고의 정체성은 유지하면서 헤어라인을 연상시키는 섬세한 디테일을 더하고, 글자의 형태와 균형을 정교하게 조정해 가독성과 세련미를 높였습니다. 대표 프로페셔널 라인은 경쟁 제품이 밀집된 마트 매대에서도 차별화될 수 있도록 새로운 용기 금형을 개발했습니다. 제품 기능별 정보 위계를 재정비하고 핵심 효능이 한눈에 인지되도록 그래픽을 구성해 제품 탐색과 선택의 편의성을 강화했습니다. 헬로버블 염모제는 ‘내가 쓰는 다이어리’ 콘셉트 아래 손글씨를 활용한 그래픽과 새로운 컬러 시스템을 적용해 친근하고 트렌디한 이미지를 구축했습니다. 또한 염색 컬러별 헤어 표현이 명확하게 드러나도록 모델 촬영을 디렉팅하여 제품 선택에 필요한 시각 정보를 강화했습니다. 이를 통해 미쟝센의 브랜드 아이덴티티를 보다 세련되게 정비하고, 용기 형태부터 정보 구조와 비주얼까지 일관되게 연결되는 제품 디자인 시스템을 구축했습니다.",
        tasks: [
            "B.I. 전문 업체와 협업한 미쟝센 로고타입 리디자인",
            "헤어라인 디테일을 반영하여 로고의 가독성 및 세련미 개선",
            "매대 주목도 향상을 위한 신규 용기 금형 개발",
            "제품별 기능을 명확하게 전달하는 정보 체계 그래픽 개발",
            "헬로버블 염모제의 콘셉트 및 패키지 디자인 리뉴얼",
            "염색 컬러별 헤어 표현을 강조한 모델 촬영 및 비주얼 디렉팅"
        ],
        contribution: [
            { label: "그래픽 및 단상자 디자인", val: 100 },
            { label: "용기패키지 디자인", val: 100 },
            { label: "비주얼 디렉팅", val: 40 }
        ],
        accentColor: "#D97706", // Mise-en-scene Orange/Amber
        slidesCount: 4,
        designPoints: {
            2: "기존 로고의 정체성을 유지하면서 헤어라인을 연상시키는 섬세한 디테일과 정제된 글자 형태를 적용해 가독성과 세련미를 높였습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2015년 10월 ~ 2015년 12월</span>",
            3: "마트 매대에서 차별화되는 신규 용기 금형을 개발하고, 제품별 기능과 핵심 효능이 한눈에 인지되도록 정보 위계와 패키지 그래픽을 재정비했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2015년 6월 ~ 2016년 4월</span>",
            4: "‘내가 쓰는 다이어리’ 라는 컨셉 아래 손글씨 그래픽과 새로운 컬러 시스템을 적용해 헬로버블의 친근하고 트렌디한 이미지를 강화했습니다.<br>각 염색 컬러의 특징과 완성된 헤어 이미지가 명확히 전달되도록 모델 스타일링과 촬영 비주얼을 디렉팅했습니다.<br><span style=\"font-size: 0.95rem; color: var(--text-secondary); display: block; margin-top: 12px; font-weight: normal;\">프로젝트 기간 : 2014년 11월 ~ 2015년 5월</span>"
        }
    }
};
// 2. Theme Controller (Fixed to Light Mode)
function initTheme() {
    document.documentElement.setAttribute("data-theme", "light");
    localStorage.setItem("theme", "light");
}

// Initialize theme on load
initTheme();

// 2b. Dynamic Noise Background Generator
function initNoiseBackground() {
    const canvas = document.createElement('canvas');
    canvas.width = 128;
    canvas.height = 128;
    const ctx = canvas.getContext('2d');
    const imgData = ctx.createImageData(canvas.width, canvas.height);
    const data = imgData.data;
    for (let i = 0; i < data.length; i += 4) {
        const val = Math.floor(Math.random() * 255);
        data[i] = val;
        data[i + 1] = val;
        data[i + 2] = val;
        data[i + 3] = 14; // Subtle grain opacity (~5.5%)
    }
    ctx.putImageData(imgData, 0, 0);
    const noiseUrl = canvas.toDataURL();
    document.documentElement.style.setProperty('--noise-pattern', `url(${noiseUrl})`);
}
initNoiseBackground();

// 3. Custom Magnet Mouse Cursor
const cursor = document.getElementById("custom-cursor");
const cursorDot = cursor.querySelector(".cursor-dot");
const cursorCircle = cursor.querySelector(".cursor-circle");

const solidIntroText = document.querySelector(".intro-text--solid");
let mouseX = 0;
let mouseY = 0;
let cursorX = 0;
let cursorY = 0;
const speed = 0.16; // Smooth interpolation speed

window.addEventListener("mousemove", (e) => {
    mouseX = e.clientX;
    mouseY = e.clientY;
});

window.addEventListener("touchmove", (e) => {
    if (e.touches && e.touches.length > 0) {
        mouseX = e.touches[0].clientX;
        mouseY = e.touches[0].clientY;
    }
}, { passive: true });

window.addEventListener("touchstart", (e) => {
    if (e.touches && e.touches.length > 0) {
        mouseX = e.touches[0].clientX;
        mouseY = e.touches[0].clientY;
        cursor.classList.add("hover-intro");
    }
}, { passive: true });

window.addEventListener("touchend", () => {
    cursor.classList.remove("hover-intro");
}, { passive: true });

function animateCursor() {
    cursorX += (mouseX - cursorX) * speed;
    cursorY += (mouseY - cursorY) * speed;

    cursorDot.style.left = `${mouseX}px`;
    cursorDot.style.top = `${mouseY}px`;

    cursorCircle.style.left = `${cursorX}px`;
    cursorCircle.style.top = `${cursorY}px`;

    // Update clip-path for solid text reveal relative to the container
    if (solidIntroText) {
        const rect = solidIntroText.getBoundingClientRect();
        const relativeX = cursorX - rect.left;
        const relativeY = cursorY - rect.top;
        const radius = cursor.classList.contains("hover-intro") ? 110 : 0;
        solidIntroText.style.clipPath = `circle(${radius}px at ${relativeX}px ${relativeY}px)`;
    }

    requestAnimationFrame(animateCursor);
}
animateCursor();

// Cursor Hover Effects for Links & Project Cards
function bindCursorEvents() {
    const interactiveElements = document.querySelectorAll("a:not(.nav-link), button, .project-row, #hero");
    interactiveElements.forEach((el) => {
        el.addEventListener("mouseenter", () => {
            if (el.classList.contains("project-row")) {
                cursor.classList.add("hover-project");
            } else {
                cursor.classList.add("hover-link");
            }
        });

        el.addEventListener("mouseleave", () => {
            cursor.classList.remove("hover-link");
            cursor.classList.remove("hover-project");
        });
    });
}
bindCursorEvents();

// Hover effect for hero intro container to expand cursor and trigger mask
const introContainer = document.getElementById("intro-container");
if (introContainer) {
    introContainer.addEventListener("mouseenter", () => {
        cursor.classList.add("hover-intro");
    });
    introContainer.addEventListener("mouseleave", () => {
        cursor.classList.remove("hover-intro");
    });
}

// 4. 3D Tilt Card Interaction
const tiltCards = document.querySelectorAll(".tilt-card");
tiltCards.forEach((card) => {
    card.addEventListener("mousemove", (e) => {
        const rect = card.getBoundingClientRect();
        const x = (e.clientX - rect.left) / rect.width - 0.5; // range: -0.5 to 0.5
        const y = (e.clientY - rect.top) / rect.height - 0.5; // range: -0.5 to 0.5

        // Tilt rotations (subtle, 10 degrees max)
        const tiltX = -y * 10;
        const tiltY = x * 10;

        card.style.transform = `rotateX(${tiltX}deg) rotateY(${tiltY}deg) scale(1.02)`;
    });

    card.addEventListener("mouseleave", () => {
        card.style.transform = "rotateX(0deg) rotateY(0deg) scale(1)";
    });
});

// 5. Text Scramble Effect
class TextScrambler {
    constructor(el) {
        this.el = el;
        this.chars = '!@#$%^&*()_+~`|}{[]:;?><,./-=';
        this.update = this.update.bind(this);
    }

    setText(newText) {
        const oldText = this.el.innerText;
        const length = Math.max(oldText.length, newText.length);
        const promise = new Promise((resolve) => this.resolve = resolve);
        this.queue = [];
        for (let i = 0; i < length; i++) {
            const from = oldText[i] || '';
            const to = newText[i] || '';
            const start = Math.floor(Math.random() * 30);
            const end = start + Math.floor(Math.random() * 30);
            this.queue.push({ from, to, start, end });
        }
        cancelAnimationFrame(this.frameRequest);
        this.frame = 0;
        this.update();
        return promise;
    }

    update() {
        let output = '';
        let complete = 0;
        for (let i = 0, n = this.queue.length; i < n; i++) {
            let { from, to, start, end, char } = this.queue[i];
            if (this.frame >= end) {
                complete++;
                output += to;
            } else if (this.frame >= start) {
                if (!char || Math.random() < 0.28) {
                    char = this.randomChar();
                    this.queue[i].char = char;
                }
                output += `<span class="scrambled">${char}</span>`;
            } else {
                output += from;
            }
        }
        this.el.innerHTML = output;
        if (complete === this.queue.length) {
            this.resolve();
        } else {
            this.frameRequest = requestAnimationFrame(this.update);
            this.frame++;
        }
    }

    randomChar() {
        return this.chars[Math.floor(Math.random() * this.chars.length)];
    }
}

const scrambleElements = document.querySelectorAll(".text-scramble");
scrambleElements.forEach((el) => {
    const originalText = el.getAttribute("data-text") || el.innerText;
    const hoverText = el.getAttribute("data-hover-text");
    const scrambler = new TextScrambler(el);

    if (hoverText) {
        el.addEventListener("mouseenter", () => {
            scrambler.setText(hoverText);
        });
        el.addEventListener("mouseleave", () => {
            scrambler.setText(originalText);
        });
    } else {
        el.addEventListener("mouseenter", () => {
            scrambler.setText(originalText);
        });
    }
});

// 6. Scroll Reveal (Intersection Observer)
const revealElements = document.querySelectorAll(".scroll-reveal");
const observerOptions = {
    root: null,
    threshold: 0.08,
    rootMargin: "0px 0px -40px 0px"
};

const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        if (entry.isIntersecting) {
            entry.target.classList.add("active");
            revealObserver.unobserve(entry.target);
        }
    });
}, observerOptions);

revealElements.forEach((el) => revealObserver.observe(el));

// 6.5. Interactive VMD & Product 3D Wireframe Animation
const radarCanvas = document.getElementById("radar-canvas");
if (radarCanvas) {
    const ctx = radarCanvas.getContext("2d");
    const radarContainer = document.querySelector(".hero-radar-container");
    let width = 0;
    let height = 0;

    // Scale for high DPI displays
    function resizeRadar() {
        const rect = radarCanvas.getBoundingClientRect();
        const dpr = window.devicePixelRatio || 1;
        width = rect.width;
        height = rect.height;
        radarCanvas.width = width * dpr;
        radarCanvas.height = height * dpr;
        ctx.scale(dpr, dpr);
    }

    window.addEventListener("resize", resizeRadar);
    resizeRadar();

    // 3D Projection math
    function rotate3D(point, pitch, yaw, roll) {
        let x = point.x, y = point.y, z = point.z;

        // Rotate X (pitch)
        let cosX = Math.cos(pitch), sinX = Math.sin(pitch);
        let y1 = y * cosX - z * sinX;
        let z1 = y * sinX + z * cosX;

        // Rotate Y (yaw)
        let cosY = Math.cos(yaw), sinY = Math.sin(yaw);
        let x2 = x * cosY + z1 * sinY;
        let z2 = -x * sinY + z1 * cosY;

        // Rotate Z (roll)
        let cosZ = Math.cos(roll), sinZ = Math.sin(roll);
        let x3 = x2 * cosZ - y1 * sinZ;
        let y3 = x2 * sinZ + y1 * cosZ;

        return { x: x3, y: y3, z: z2 };
    }

    // 1. VMD Cube Vertices (Space/Installation display)
    const cubeVertices = [];
    const size = 36;
    for (let x of [-1, 1]) {
        for (let y of [-1, 1]) {
            for (let z of [-1, 1]) {
                cubeVertices.push({ x: x * size, y: y * size, z: z * size });
            }
        }
    }
    const cubeEdges = [
        [0, 1], [1, 3], [3, 2], [2, 0], // front
        [4, 5], [5, 7], [7, 6], [6, 4], // back
        [0, 4], [1, 5], [2, 6], [3, 7]  // pillars
    ];

    // 2. Product Cylinder Vertices (Container/Packaging design)
    const cylVertices = [];
    const segments = 12;
    const cylHeight = 72;
    const radius = 28;
    // Bottom circle
    for (let i = 0; i < segments; i++) {
        let angle = (i / segments) * Math.PI * 2;
        cylVertices.push({ x: Math.cos(angle) * radius, y: -cylHeight / 2, z: Math.sin(angle) * radius });
    }
    // Top circle
    for (let i = 0; i < segments; i++) {
        let angle = (i / segments) * Math.PI * 2;
        cylVertices.push({ x: Math.cos(angle) * radius, y: cylHeight / 2, z: Math.sin(angle) * radius });
    }
    const cylEdges = [];
    for (let i = 0; i < segments; i++) {
        cylEdges.push([i, (i + 1) % segments]);
        cylEdges.push([i + segments, ((i + 1) % segments) + segments]);
        cylEdges.push([i, i + segments]); // pillars
    }

    let pitch1 = 0, yaw1 = 0, roll1 = 0;
    let pitch2 = 0, yaw2 = 0, roll2 = 0;

    let mouseXRaw = 0;
    let mouseYRaw = 0;
    let targetRotateX = 0;
    let targetRotateY = 0;
    let currentRotateX = 0;
    let currentRotateY = 0;

    let vmdHoverForce = 0;
    let prodHoverForce = 0;

    // Track mouse coordinates
    window.addEventListener("mousemove", (e) => {
        const rect = radarCanvas.getBoundingClientRect();
        mouseXRaw = e.clientX - rect.left;
        mouseYRaw = e.clientY - rect.top;

        const x = e.clientX / window.innerWidth - 0.5;
        const y = e.clientY / window.innerHeight - 0.5;

        targetRotateX = -y * 20; // Parallax tilt bounds
        targetRotateY = x * 20;
    });

    function drawWireframe(vertices, edges, center, pitch, yaw, roll, hoverForce, label, isCube) {
        const isDark = document.documentElement.getAttribute("data-theme") === "dark";
        const strokeColor = isDark ? "rgba(229, 193, 88, 0.15)" : "rgba(30, 30, 30, 0.15)";
        const guideColor = isDark ? "rgba(229, 193, 88, 0.03)" : "rgba(30, 30, 30, 0.03)";

        // 3D Perspective Projection settings
        const cameraZ = 250;
        const projected = vertices.map(v => {
            let vx = v.x;
            let vy = v.y;
            let vz = v.z;

            // Explosion/separation morphing effect on hover
            if (hoverForce > 0) {
                const morph = 1 + hoverForce * 0.35;
                vx *= morph;
                vy *= morph;
                vz *= morph;
            }

            let p = rotate3D({ x: vx, y: vy, z: vz }, pitch, yaw, roll);
            let scale = cameraZ / (cameraZ + p.z);
            return {
                x: center.x + p.x * scale,
                y: center.y + p.y * scale,
                z: p.z
            };
        });

        // Draw surrounding radar ring / guide line
        ctx.strokeStyle = guideColor;
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.arc(center.x, center.y, 75 + hoverForce * 15, 0, Math.PI * 2);
        ctx.stroke();

        // Draw dotted outer ring if hovered
        if (hoverForce > 0.05) {
            ctx.strokeStyle = isDark ? `rgba(229, 193, 88, ${hoverForce * 0.12})` : `rgba(30, 30, 30, ${hoverForce * 0.12})`;
            ctx.setLineDash([4, 6]);
            ctx.beginPath();
            ctx.arc(center.x, center.y, 105, 0, Math.PI * 2);
            ctx.stroke();
            ctx.setLineDash([]);
        }

        // Draw wireframe edges
        edges.forEach(edge => {
            let p1 = projected[edge[0]];
            let p2 = projected[edge[1]];

            let avgZ = (p1.z + p2.z) / 2;
            let depthFactor = (avgZ + 60) / 120; // Normalization
            depthFactor = Math.max(0, Math.min(1, depthFactor));

            // Fading lines based on Z-depth
            let alpha = (0.12 + (1 - depthFactor) * 0.38) * (1 - hoverForce * 0.35);
            let color = isDark
                ? `rgba(229, 193, 88, ${alpha + hoverForce * 0.6})`
                : `rgba(30, 30, 30, ${alpha + hoverForce * 0.6})`;

            ctx.strokeStyle = color;
            ctx.lineWidth = 1.0 + hoverForce * 0.8;
            ctx.beginPath();
            ctx.moveTo(p1.x, p1.y);
            ctx.lineTo(p2.x, p2.y);
            ctx.stroke();
        });

        // Draw vertex nodes
        projected.forEach(p => {
            let nodeAlpha = 0.1 + hoverForce * 0.65;
            ctx.fillStyle = isDark ? `rgba(229, 193, 88, ${nodeAlpha})` : `rgba(30, 30, 30, ${nodeAlpha})`;
            ctx.beginPath();
            ctx.arc(p.x, p.y, 2 + hoverForce * 1.5, 0, Math.PI * 2);
            ctx.fill();
        });

        // Bounding projection lines on hover
        if (hoverForce > 0.15) {
            ctx.strokeStyle = isDark ? `rgba(229, 193, 88, ${hoverForce * 0.2})` : `rgba(30, 30, 30, ${hoverForce * 0.2})`;
            ctx.lineWidth = 0.5;
            if (isCube) {
                // Ground projection plane
                ctx.beginPath();
                ctx.moveTo(projected[0].x, projected[0].y + 35);
                ctx.lineTo(projected[1].x, projected[1].y + 35);
                ctx.lineTo(projected[3].x, projected[3].y + 35);
                ctx.lineTo(projected[2].x, projected[2].y + 35);
                ctx.closePath();
                ctx.stroke();
            } else {
                // Central axis
                ctx.beginPath();
                ctx.moveTo(center.x, center.y - 65);
                ctx.lineTo(center.x, center.y + 65);
                ctx.stroke();
            }
        }

        // Draw label (Uppercase)
        let textAlpha = 0.35 + hoverForce * 0.65;
        ctx.fillStyle = isDark ? `rgba(229, 193, 88, ${textAlpha})` : `rgba(30, 30, 30, ${textAlpha})`;
        ctx.font = "800 9.5px 'Questrial', sans-serif";
        ctx.textAlign = "center";
        ctx.fillText(label, center.x, center.y + 105 + hoverForce * 8);

        // Blueprint info text on hover
        if (hoverForce > 0.3) {
            ctx.fillStyle = isDark ? `rgba(229, 193, 88, ${hoverForce * 0.3})` : `rgba(30, 30, 30, ${hoverForce * 0.3})`;
            ctx.font = "500 7px 'Questrial', sans-serif";
            const currentScale = (1.0 + hoverForce * 0.35).toFixed(2);
            ctx.fillText(`SYS.SCALE: ${currentScale}X / 3D.MESH`, center.x, center.y + 120);
        }
    }

    function drawRadar() {
        if (!radarCanvas.getContext) return;

        // Clear frame
        ctx.clearRect(0, 0, width, height);

        // Parallax smooth interpolation
        currentRotateX += (targetRotateX - currentRotateX) * 0.08;
        currentRotateY += (targetRotateY - currentRotateY) * 0.08;

        if (radarContainer) {
            radarContainer.style.transform = `perspective(1000px) rotateX(${currentRotateX}deg) rotateY(${currentRotateY}deg)`;
        }

        // Layout calculations
        const vmdCenter = { x: 0, y: 0 };
        const prodCenter = { x: 0, y: 0 };

        if (width > 1024) {
            vmdCenter.x = width * 0.55;
            vmdCenter.y = height * 0.45;
            prodCenter.x = width * 0.8;
            prodCenter.y = height * 0.55;
        } else {
            vmdCenter.x = width * 0.5;
            vmdCenter.y = height * 0.28;
            prodCenter.x = width * 0.5;
            prodCenter.y = height * 0.72;
        }

        // Calculate hover distance
        const distToVMD = Math.sqrt(Math.pow(mouseXRaw - vmdCenter.x, 2) + Math.pow(mouseYRaw - vmdCenter.y, 2));
        const distToProd = Math.sqrt(Math.pow(mouseXRaw - prodCenter.x, 2) + Math.pow(mouseYRaw - prodCenter.y, 2));

        const targetVMDHover = distToVMD < 140 ? 1 : 0;
        const targetProdHover = distToProd < 140 ? 1 : 0;

        // Smooth hover interpolation
        vmdHoverForce += (targetVMDHover - vmdHoverForce) * 0.1;
        prodHoverForce += (targetProdHover - prodHoverForce) * 0.1;

        // Automatic rotation speed increases when hovered
        pitch1 += 0.003 + vmdHoverForce * 0.007;
        yaw1 += 0.005 + vmdHoverForce * 0.012;
        roll1 += 0.002;

        pitch2 += 0.004 + prodHoverForce * 0.008;
        yaw2 += 0.003 + prodHoverForce * 0.01;
        roll2 += 0.001;

        // Draw VMD Spatial Cube
        drawWireframe(cubeVertices, cubeEdges, vmdCenter, pitch1, yaw1, roll1, vmdHoverForce, "VMD / SPACE DESIGN", true);

        // Draw Product Cylinder
        drawWireframe(cylVertices, cylEdges, prodCenter, pitch2, yaw2, roll2, prodHoverForce, "PRODUCT / PACKAGING DESIGN", false);

        requestAnimationFrame(drawRadar);
    }

    requestAnimationFrame(drawRadar);
}

// 7. Case Study Drawer & Dynamic Accent Binding
const drawer = document.getElementById("case-study-drawer");
const drawerCloseBtn = drawer.querySelector(".drawer__close-btn");
const galleryWrapper = document.getElementById("drawer-gallery");

const drawerCat = document.getElementById("drawer-project-cat");
const drawerTitle = document.getElementById("drawer-project-title");
const drawerPeriod = document.getElementById("drawer-period");
const drawerRole = document.getElementById("drawer-role");
const drawerDesc = document.getElementById("drawer-desc");
const drawerTasks = document.getElementById("drawer-tasks");
const drawerContribution = document.getElementById("drawer-contribution");

let currentProjectKey = "";

// Project Card Clicks
const projectRows = document.querySelectorAll(".project-row");
projectRows.forEach((row) => {
    row.addEventListener("click", (e) => {
        e.preventDefault();
        const projectKey = row.getAttribute("data-project");
        if (projectKey === "ap_beauty_wordmark") {
            window.open("https://design.amorepacific.com/work/2026/ap-beauty/wordmark/", "_blank");
        } else {
            window.location.hash = "project-" + projectKey;
        }
    });
});

function openProjectDrawer(key) {
    const data = projectData[key];
    if (!data) return;

    currentProjectKey = key;
    activeSlideIndex = 0;
    totalSlides = data.slidesCount - 1;

    drawer.setAttribute("data-project-key", key);

    // Set Dynamic Accent Color
    document.documentElement.style.setProperty("--brand-accent", data.accentColor);

    // Bind Text Content
    drawerCat.innerText = data.cat;
    drawerTitle.innerText = data.title;
    drawerPeriod.innerText = data.period;
    drawerRole.innerText = data.role;
    drawerDesc.innerText = data.desc;

    // Render Stores list if exists (below OVERVIEW with gray line)
    const existingStores = document.getElementById("drawer-stores-section");
    if (existingStores) existingStores.remove();

    if (data.stores && data.stores.length > 0) {
        const storesSection = document.createElement("div");
        storesSection.id = "drawer-stores-section";
        storesSection.style.marginTop = "36px";
        storesSection.style.borderTop = "1px solid var(--border-color)";
        storesSection.style.paddingTop = "20px";

        const storesList = document.createElement("ul");
        storesList.style.listStyle = "none";
        storesList.style.padding = "0";
        storesList.style.margin = "0";

        data.stores.forEach(store => {
            const li = document.createElement("li");
            li.innerText = "- " + store;
            li.style.fontSize = "0.88rem";
            li.style.lineHeight = "1.6";
            li.style.color = "var(--text-secondary)";
            li.style.marginBottom = "2px";
            storesList.appendChild(li);
        });

        storesSection.appendChild(storesList);

        // Insert after the OVERVIEW detail-section (which contains drawer-desc)
        const overviewParent = drawerDesc.closest(".detail-section");
        if (overviewParent) {
            overviewParent.after(storesSection);
        }
    }

    // Render Contribution Chart
    drawerContribution.innerHTML = "";
    data.contribution.forEach((item, idx) => {
        const itemDiv = document.createElement("div");
        itemDiv.className = "contribution-bar";

        const labelDiv = document.createElement("div");
        labelDiv.className = "contribution-bar__label";

        const nameSpan = document.createElement("span");
        nameSpan.innerText = item.label;

        const pctSpan = document.createElement("span");
        pctSpan.className = "bar-pct";
        pctSpan.innerText = "0%";

        labelDiv.appendChild(nameSpan);
        labelDiv.appendChild(pctSpan);

        const bgDiv = document.createElement("div");
        bgDiv.className = "contribution-bar__bg";

        const fillDiv = document.createElement("div");
        fillDiv.className = "contribution-bar__fill";

        bgDiv.appendChild(fillDiv);
        itemDiv.appendChild(labelDiv);
        itemDiv.appendChild(bgDiv);
        drawerContribution.appendChild(itemDiv);

        // Staggered animation: each bar starts after 120ms delay
        const delay = 180 + idx * 120;

        setTimeout(() => {
            // Trigger bar fill
            fillDiv.style.width = `${item.val}%`;
            itemDiv.classList.add("bar-filled");

            // Count-up number animation
            const duration = 1400; // ms, matches CSS transition
            const startTime = performance.now();
            const targetVal = item.val;

            function animateCount(currentTime) {
                const elapsed = currentTime - startTime;
                const progress = Math.min(elapsed / duration, 1);
                // easeOutExpo for dramatic feel
                const eased = progress === 1 ? 1 : 1 - Math.pow(2, -10 * progress);
                const current = Math.round(eased * targetVal);
                pctSpan.innerText = `${current}%`;

                if (progress < 1) {
                    requestAnimationFrame(animateCount);
                } else {
                    pctSpan.innerText = `${targetVal}%`;
                }
            }
            requestAnimationFrame(animateCount);
        }, delay);
    });

    // Populate Task Bullets
    drawerTasks.innerHTML = "";
    data.tasks.forEach((task) => {
        const li = document.createElement("li");
        li.innerHTML = task;
        drawerTasks.appendChild(li);
    });

    // Populate Gallery Images (Page 1 info slide skipped, rendering from Page 2)
    galleryWrapper.innerHTML = "";

    for (let i = 2; i <= data.slidesCount; i++) {
        // Skip the first slide (black line graphic) for AP Beauty project as requested
        if (key === "ap_beauty" && i === 2) {
            continue;
        }
        const slide = document.createElement("div");
        slide.className = "gallery-slide";

        if (key === "promotion_mbs" && i === 8) {
            slide.style.marginTop = "200px";
        }

        const pageMeta = (imageMetadata[key] && imageMetadata[key][i]) ? imageMetadata[key][i] : null;

        if (pageMeta) {
            // Background images removed as requested by user (replaced by code-based shimmering noise)

            if (pageMeta.type === "split-2-1") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-layout", "split-2-1");

                const ars = pageMeta.ars || [1.5, 1.5, 1.5];
                const r1 = ars[0];
                const r2 = ars[1];
                const r3 = ars[2];

                const K = (1 / r1) + (1 / r2);
                const denom = 1 + r3 * K;
                const leftWidth = `calc((100% - ${16 * (1 + r3)}px) / ${denom})`;
                const rightWidth = `calc(100% - 16px - ${leftWidth})`;

                // Create Left Column
                const leftCol = document.createElement("div");
                leftCol.className = "grid-col grid-col--left";
                leftCol.style.width = leftWidth;
                leftCol.style.flex = `0 0 ${leftWidth}`;
                leftCol.style.display = "flex";
                leftCol.style.flexDirection = "column";
                leftCol.style.gap = "16px";

                const img1 = document.createElement("img");
                img1.src = pageMeta.photos[0];
                img1.alt = `${data.title} Page ${i} Image 1`;
                img1.loading = "lazy";
                img1.style.width = "100%";
                img1.style.height = "auto";
                leftCol.appendChild(img1);

                const img2 = document.createElement("img");
                img2.src = pageMeta.photos[1];
                img2.alt = `${data.title} Page ${i} Image 2`;
                img2.loading = "lazy";
                img2.style.width = "100%";
                img2.style.height = "auto";
                leftCol.appendChild(img2);

                // Create Right Column
                const rightCol = document.createElement("div");
                rightCol.className = "grid-col grid-col--right";
                rightCol.style.width = rightWidth;
                rightCol.style.flex = `0 0 ${rightWidth}`;
                rightCol.style.display = "flex";
                rightCol.style.flexDirection = "column";

                const img3 = document.createElement("img");
                img3.src = pageMeta.photos[2];
                img3.alt = `${data.title} Page ${i} Image 3`;
                img3.loading = "lazy";
                img3.style.width = "100%";
                img3.style.height = "100%";
                img3.style.objectFit = "cover";
                rightCol.appendChild(img3);

                grid.appendChild(leftCol);
                grid.appendChild(rightCol);
                container.appendChild(grid);
                slide.appendChild(container);
            } else if (pageMeta.type === "split-1-2") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-layout", "split-1-2");

                const ars = pageMeta.ars || [1.5, 1.5, 1.5];
                const r1 = ars[0]; // leftmost
                const r2 = ars[1]; // middle
                const r3 = ars[2]; // rightmost

                const K = (1 / r3) + (1 / r2);
                const denom = 1 + (1 / (r1 * K));
                const leftWidth = `calc((100% - ${16 * (1 - (1 / K))}px) / ${denom})`;
                const rightWidth = `calc(100% - 16px - ${leftWidth})`;

                // Create Left Column
                const leftCol = document.createElement("div");
                leftCol.className = "grid-col grid-col--left";
                leftCol.style.width = leftWidth;
                leftCol.style.flex = `0 0 ${leftWidth}`;
                leftCol.style.display = "flex";
                leftCol.style.flexDirection = "column";

                const img1 = document.createElement("img");
                img1.src = pageMeta.photos[0]; // leftmost
                img1.alt = `${data.title} Page ${i} Image 1`;
                img1.loading = "lazy";
                img1.style.width = "100%";
                img1.style.height = "100%";
                img1.style.objectFit = "cover";
                leftCol.appendChild(img1);

                // Create Right Column
                const rightCol = document.createElement("div");
                rightCol.className = "grid-col grid-col--right";
                rightCol.style.width = rightWidth;
                rightCol.style.flex = `0 0 ${rightWidth}`;
                rightCol.style.display = "flex";
                rightCol.style.flexDirection = "column";
                rightCol.style.gap = "16px";

                const img2 = document.createElement("img");
                img2.src = pageMeta.photos[2]; // rightmost on top
                img2.alt = `${data.title} Page ${i} Image 2`;
                img2.loading = "lazy";
                img2.style.width = "100%";
                img2.style.height = "auto";
                rightCol.appendChild(img2);

                const img3 = document.createElement("img");
                img3.src = pageMeta.photos[1]; // middle on bottom
                img3.alt = `${data.title} Page ${i} Image 3`;
                img3.loading = "lazy";
                img3.style.width = "100%";
                img3.style.height = "auto";
                rightCol.appendChild(img3);

                grid.appendChild(leftCol);
                grid.appendChild(rightCol);
                container.appendChild(grid);
                slide.appendChild(container);
            } else if (pageMeta.type === "vertical") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-layout", "vertical");
                grid.style.display = "flex";
                grid.style.flexDirection = "column";
                grid.style.gap = "24px";
                grid.style.width = "100%";

                pageMeta.photos.forEach((photoPath, idx) => {
                    const img = document.createElement("img");
                    img.src = photoPath;
                    img.alt = `${data.title} Page ${i} Image ${idx + 1}`;
                    img.loading = "lazy";
                    img.style.width = "100%";
                    img.style.height = "auto";
                    grid.appendChild(img);
                });

                container.appendChild(grid);
                slide.appendChild(container);
            } else if (pageMeta.type === "split-1-3") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-layout", "split-1-3");

                const ars = pageMeta.ars || [1.5, 1.5, 1.5, 1.5];
                const rL = ars[0];
                const r1 = ars[1];
                const r2 = ars[2];
                const r3 = ars[3];

                const K = (1 / r1) + (1 / r2) + (1 / r3);
                const denom = 1 + (1 / (rL * K));
                const leftWidth = `calc((100% - ${16 * (1 - (2 / K))}px) / ${denom})`;
                const rightWidth = `calc(100% - 16px - ${leftWidth})`;

                // Create Left Column
                const leftCol = document.createElement("div");
                leftCol.className = "grid-col grid-col--left";
                leftCol.style.width = leftWidth;
                leftCol.style.flex = `0 0 ${leftWidth}`;
                leftCol.style.display = "flex";
                leftCol.style.flexDirection = "column";

                const img1 = document.createElement("img");
                img1.src = pageMeta.photos[0];
                img1.alt = `${data.title} Page ${i} Image 1`;
                img1.loading = "lazy";
                img1.style.width = "100%";
                img1.style.height = "100%";
                img1.style.objectFit = "cover";
                leftCol.appendChild(img1);

                // Create Right Column
                const rightCol = document.createElement("div");
                rightCol.className = "grid-col grid-col--right";
                rightCol.style.width = rightWidth;
                rightCol.style.flex = `0 0 ${rightWidth}`;
                rightCol.style.display = "flex";
                rightCol.style.flexDirection = "column";
                rightCol.style.gap = "16px";

                for (let j = 1; j <= 3; j++) {
                    const img = document.createElement("img");
                    img.src = pageMeta.photos[j];
                    img.alt = `${data.title} Page ${i} Image ${j + 1}`;
                    img.loading = "lazy";
                    img.style.width = "100%";
                    img.style.height = "auto";
                    rightCol.appendChild(img);
                }

                grid.appendChild(leftCol);
                grid.appendChild(rightCol);
                container.appendChild(grid);
                slide.appendChild(container);
            } else if (pageMeta.type === "split-1-subgrid") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-layout", "split-1-subgrid");

                const ars = pageMeta.ars || [1, 1, 1, 1];
                const rL = ars[0];
                const rR1 = ars[1];
                const rR2 = ars[2];
                const rR3 = ars[3];

                const sumR23 = rR2 + rR3;
                const K = (1 / rR1) + (1 / sumR23);
                const term1 = `(100% - 16px) * ${K}`;
                const term2 = `${16 * (1 - (1 / sumR23))}px`;
                const denom = (1 / rL) + K;
                const leftWidth = `calc((${term1} + ${term2}) / ${denom})`;
                const rightWidth = `calc(100% - 16px - ${leftWidth})`;

                // Create Left Column
                const leftCol = document.createElement("div");
                leftCol.className = "grid-col grid-col--left";
                leftCol.style.width = leftWidth;
                leftCol.style.flex = `0 0 ${leftWidth}`;
                leftCol.style.display = "flex";
                leftCol.style.flexDirection = "column";

                const img1 = document.createElement("img");
                img1.src = pageMeta.photos[0];
                img1.alt = `${data.title} Page ${i} Image 1`;
                img1.loading = "lazy";
                img1.style.width = "100%";
                img1.style.height = "100%";
                img1.style.objectFit = "cover";
                leftCol.appendChild(img1);

                // Create Right Column
                const rightCol = document.createElement("div");
                rightCol.className = "grid-col grid-col--right";
                rightCol.style.width = rightWidth;
                rightCol.style.flex = `0 0 ${rightWidth}`;
                rightCol.style.display = "flex";
                rightCol.style.flexDirection = "column";
                rightCol.style.gap = "16px";

                // Top image in right column
                const img2 = document.createElement("img");
                img2.src = pageMeta.photos[1];
                img2.alt = `${data.title} Page ${i} Image 2`;
                img2.loading = "lazy";
                img2.style.width = "100%";
                img2.style.height = "auto";
                rightCol.appendChild(img2);

                // Bottom row in right column containing two side-by-side images
                const bottomRow = document.createElement("div");
                bottomRow.className = "subgrid-bottom-row";
                bottomRow.style.display = "flex";
                bottomRow.style.gap = "16px";
                bottomRow.style.width = "100%";

                const w2 = `calc((100% - 16px) * ${rR2} / ${sumR23})`;
                const w3 = `calc(100% - 16px - ${w2})`;

                const img3 = document.createElement("img");
                img3.src = pageMeta.photos[2];
                img3.alt = `${data.title} Page ${i} Image 3`;
                img3.loading = "lazy";
                img3.style.width = w2;
                img3.style.flex = `0 0 ${w2}`;
                img3.style.height = "auto";
                bottomRow.appendChild(img3);

                const img4 = document.createElement("img");
                img4.src = pageMeta.photos[3];
                img4.alt = `${data.title} Page ${i} Image 4`;
                img4.loading = "lazy";
                img4.style.width = w3;
                img4.style.flex = `0 0 ${w3}`;
                img4.style.height = "auto";
                bottomRow.appendChild(img4);

                rightCol.appendChild(bottomRow);

                grid.appendChild(leftCol);
                grid.appendChild(rightCol);
                container.appendChild(grid);
                slide.appendChild(container);
            } else if (pageMeta.type === "split-3col-leftstacked") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-layout", "split-3col-leftstacked");

                const ars = pageMeta.ars || [1, 1, 1, 1];
                const r1 = ars[0]; // left top
                const r2 = ars[1]; // left bottom
                const r3 = ars[2]; // center
                const r4 = ars[3]; // right

                const A = 1 / ((1 / r1) + (1 / r2));
                const denom = A + r3 + r4;
                const H = `calc((100% - ${16 * (2 - A)}px) / ${denom})`;

                const w1 = `calc((${H} - 16px) * ${A})`;
                const w2 = `calc(${H} * ${r3})`;
                const w3 = `calc(${H} * ${r4})`;

                // Column 1 (Left stacked)
                const col1 = document.createElement("div");
                col1.className = "grid-col";
                col1.style.width = w1;
                col1.style.flex = `0 0 ${w1}`;
                col1.style.display = "flex";
                col1.style.flexDirection = "column";
                col1.style.gap = "16px";

                const img1 = document.createElement("img");
                img1.src = pageMeta.photos[0];
                img1.alt = `${data.title} Page ${i} Image 1`;
                img1.loading = "lazy";
                img1.style.width = "100%";
                img1.style.height = "auto";
                col1.appendChild(img1);

                const img2 = document.createElement("img");
                img2.src = pageMeta.photos[1];
                img2.alt = `${data.title} Page ${i} Image 2`;
                img2.loading = "lazy";
                img2.style.width = "100%";
                img2.style.height = "auto";
                col1.appendChild(img2);

                // Column 2 (Center)
                const col2 = document.createElement("div");
                col2.className = "grid-col";
                col2.style.width = w2;
                col2.style.flex = `0 0 ${w2}`;
                col2.style.display = "flex";
                col2.style.flexDirection = "column";

                const img3 = document.createElement("img");
                img3.src = pageMeta.photos[2];
                img3.alt = `${data.title} Page ${i} Image 3`;
                img3.loading = "lazy";
                img3.style.width = "100%";
                img3.style.height = "100%";
                img3.style.objectFit = "cover";
                col2.appendChild(img3);

                // Column 3 (Right)
                const col3 = document.createElement("div");
                col3.className = "grid-col";
                col3.style.width = w3;
                col3.style.flex = `0 0 ${w3}`;
                col3.style.display = "flex";
                col3.style.flexDirection = "column";

                const img4 = document.createElement("img");
                img4.src = pageMeta.photos[3];
                img4.alt = `${data.title} Page ${i} Image 4`;
                img4.loading = "lazy";
                img4.style.width = "100%";
                img4.style.height = "100%";
                img4.style.objectFit = "cover";
                col3.appendChild(img4);

                grid.appendChild(col1);
                grid.appendChild(col2);
                grid.appendChild(col3);
                container.appendChild(grid);
                slide.appendChild(container);
            } else if (pageMeta.type === "grid" || pageMeta.type === "single") {
                const container = document.createElement("div");
                container.className = "slide-photos-container";

                const grid = document.createElement("div");
                grid.className = "photos-layout-grid";
                grid.setAttribute("data-count", pageMeta.photos.length);

                const photosCount = pageMeta.photos.length;
                const ars = pageMeta.ars || [];
                const gap = pageMeta.gap !== undefined ? pageMeta.gap : 16;
                grid.style.gap = `${gap}px`;

                // Group images into rows based on layout design
                let rows = [];
                if (pageMeta.rows) {
                    rows = pageMeta.rows;
                } else if (photosCount === 1) {
                    rows = [[0]];
                } else if (photosCount === 2) {
                    rows = [[0, 1]];
                } else if (photosCount === 3) {
                    rows = [[0, 1, 2]];
                } else if (photosCount === 4) {
                    rows = [[0, 1], [2, 3]];
                } else if (photosCount === 5) {
                    rows = [[0, 1], [2, 3, 4]];
                } else {
                    for (let idx = 0; idx < photosCount; idx += 3) {
                        const row = [];
                        for (let j = 0; j < 3 && idx + j < photosCount; j++) {
                            row.push(idx + j);
                        }
                        rows.push(row);
                    }
                }

                // Pre-calculate exact width styles for each image
                const indexStyles = {};
                rows.forEach(row => {
                    const N = row.length;
                    const sumOfArs = row.reduce((sum, imgIdx) => sum + (ars[imgIdx] || 1.5), 0);
                    row.forEach(imgIdx => {
                        const ar = ars[imgIdx] || 1.5;
                        const widthPct = `calc((100% - ${(N - 1) * gap}px) * ${ar} / ${sumOfArs})`;
                        indexStyles[imgIdx] = {
                            width: widthPct,
                            flex: `0 0 ${widthPct}`
                        };
                    });
                });

                pageMeta.photos.forEach((photoPath, idx) => {
                    const img = document.createElement("img");
                    img.src = photoPath;
                    img.alt = `${data.title} Page ${i}`;
                    img.loading = "lazy";

                    const styleRule = indexStyles[idx];
                    if (styleRule) {
                        img.style.width = styleRule.width;
                        img.style.flex = styleRule.flex;
                    }

                    grid.appendChild(img);
                });

                container.appendChild(grid);
                slide.appendChild(container);
            } else {
                slide.classList.add("gallery-slide--legacy");
                const img = document.createElement("img");
                img.src = pageMeta.photos[0] || `asset/${key}/page_${i}.jpg`;
                img.alt = `${data.title} Page ${i}`;
                img.loading = "lazy";
                slide.appendChild(img);
            }
        } else {
            slide.classList.add("gallery-slide--legacy");
            const img = document.createElement("img");
            img.src = `asset/${key}/page_${i}.jpg`;
            img.alt = `${data.title} Page ${i}`;
            img.loading = "lazy";
            slide.appendChild(img);
        }

        // Append design story link below the first image of AP Beauty project (Slide 3)
        if (key === "ap_beauty" && i === 3) {
            const linkContainer = document.createElement("div");
            linkContainer.className = "design-story-link-container";
            linkContainer.innerHTML = `
                <a href="https://design.amorepacific.com/work/2024/ap-beauty/beauty-new-s-i-design/" 
                   target="_blank" 
                   rel="noopener noreferrer" 
                   class="design-story-link">디자인 스토리 상세 보기 : Amorepacific Creatives<svg class="link-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; vertical-align: middle; margin-left: 4px; transition: transform 0.2s ease;"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
            `;
            slide.appendChild(linkContainer);
        }
        // Append design story link below the first image of Aritaum Shanghai project (Slide 2)
        if (key === "aritaum_shanghai" && i === 2) {
            const linkContainer = document.createElement("div");
            linkContainer.className = "design-story-link-container";
            linkContainer.innerHTML = `
                <a href="https://design.amorepacific.com/work/2020/aritaum/aritaum-shanghai/" 
                   target="_blank" 
                   rel="noopener noreferrer" 
                   class="design-story-link">디자인 스토리 상세 보기 : Amorepacific Creatives<svg class="link-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; vertical-align: middle; margin-left: 4px; transition: transform 0.2s ease;"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
            `;
            slide.appendChild(linkContainer);
        }
        // Append design story link above the design point of Promotion & MBS project (Slide 8)
        if (key === "promotion_mbs" && i === 8) {
            const linkContainer = document.createElement("div");
            linkContainer.className = "design-story-link-container";
            linkContainer.innerHTML = `
                <a href="https://design.amorepacific.com/work/2025/ap-beauty/holiday-campain-unveil-your-beauty-beyond/" 
                   target="_blank" 
                   rel="noopener noreferrer" 
                   class="design-story-link">디자인 스토리 상세 보기 : Amorepacific Creatives<svg class="link-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; vertical-align: middle; margin-left: 4px; transition: transform 0.2s ease;"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
            `;
            slide.appendChild(linkContainer);
        }
        // Append design story link below Slide 12 of AP Beauty project (MD Upgrade)
        if (key === "ap_beauty" && i === 12) {
            const linkContainer = document.createElement("div");
            linkContainer.className = "design-story-link-container";
            linkContainer.innerHTML = `
                <a href="https://design.amorepacific.com/work/2026/ap-beauty/si-design-upgrade/" 
                   target="_blank" 
                   rel="noopener noreferrer" 
                   class="design-story-link">디자인 스토리 상세 보기 : Amorepacific Creatives<svg class="link-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; vertical-align: middle; margin-left: 4px; transition: transform 0.2s ease;"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
            `;
            slide.appendChild(linkContainer);
        }
        // Append design story link below Slide 21 of AP Beauty project (Rejuvenating Booster Shot MD Serum)
        if (key === "ap_beauty" && i === 21) {
            const linkContainer = document.createElement("div");
            linkContainer.className = "design-story-link-container";
            linkContainer.innerHTML = `
                <a href="https://design.amorepacific.com/work/2026/ap-beauty/retail-design-for-the-launch-of-rejuvenating-booster-shot-md-serum/" 
                   target="_blank" 
                   rel="noopener noreferrer" 
                   class="design-story-link">디자인 스토리 상세 보기 : Amorepacific Creatives<svg class="link-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; vertical-align: middle; margin-left: 4px; transition: transform 0.2s ease;"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
            `;
            slide.appendChild(linkContainer);
        }
        // Append design story link below the first image of AP Beauty Wordmark project (Slide 2)
        if (key === "ap_beauty_wordmark" && i === 2) {
            const linkContainer = document.createElement("div");
            linkContainer.className = "design-story-link-container";
            linkContainer.innerHTML = `
                <a href="https://design.amorepacific.com/work/2026/ap-beauty/wordmark/" 
                   target="_blank" 
                   rel="noopener noreferrer" 
                   class="design-story-link">디자인 스토리 상세 보기 : Amorepacific Creatives<svg class="link-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display: inline-block; vertical-align: middle; margin-left: 4px; transition: transform 0.2s ease;"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
            `;
            slide.appendChild(linkContainer);
        }

        // Append design point caption below image container (and below design story link if present)
        if (data.designPoints && data.designPoints[i]) {
            const caption = document.createElement("div");
            caption.className = "design-point-caption";
            caption.innerHTML = `
                <span class="dp-caption-tag">DESIGN POINT</span>
                <p class="dp-caption-text">${data.designPoints[i]}</p>
            `;
            slide.appendChild(caption);
        }

        galleryWrapper.appendChild(slide);
    }

    // Open Drawer & Reset Scroll position to top
    drawer.classList.add("active");
    document.body.style.overflow = "hidden"; // Prevent background scrolling

    // Reset scroll position reliably after drawer opens and DOM registers heights
    galleryWrapper.scrollTop = 0;
    const infoPanel = document.querySelector(".drawer__info-panel");
    if (infoPanel) {
        infoPanel.scrollTop = 0;
    }
    const scrollContainer = document.querySelector(".drawer__scroll-container");
    if (scrollContainer) {
        scrollContainer.scrollTop = 0;
    }
    setTimeout(() => {
        galleryWrapper.scrollTop = 0;
        if (infoPanel) {
            infoPanel.scrollTop = 0;
        }
        if (scrollContainer) {
            scrollContainer.scrollTop = 0;
        }
    }, 50);

    // Bind cursor hover inside drawer
    bindCursorEvents();
}

// Close Drawer (updates hash/history)
function closeProjectDrawer() {
    if (window.location.hash.startsWith("#project-")) {
        history.pushState(null, null, window.location.pathname + window.location.search);
    }
    closeProjectDrawerInternal();
}

// Close Drawer Internal (handles visual state reset)
function closeProjectDrawerInternal() {
    drawer.classList.remove("active");
    drawer.removeAttribute("data-project-key");
    document.body.style.overflow = ""; // Enable background scrolling

    // Reset accent color
    document.documentElement.style.removeProperty("--brand-accent");

    // Reset cursor state
    cursor.classList.remove("hover-project");
    cursor.classList.remove("hover-link");

    // Reset gallery scroll position immediately on close
    galleryWrapper.scrollTop = 0;
    const infoPanel = document.querySelector(".drawer__info-panel");
    if (infoPanel) {
        infoPanel.scrollTop = 0;
    }
    const scrollContainer = document.querySelector(".drawer__scroll-container");
    if (scrollContainer) {
        scrollContainer.scrollTop = 0;
    }

    currentProjectKey = "";
}

drawerCloseBtn.addEventListener("click", closeProjectDrawer);

// Close on ESC Key
window.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && drawer.classList.contains("active")) {
        closeProjectDrawer();
    }
});

// Hash Router for Project Drawers
function checkHashAndRoute() {
    const hash = window.location.hash;
    if (hash.startsWith("#project-")) {
        const key = hash.replace("#project-", "");
        if (projectData[key]) {
            if (key === "ap_beauty_wordmark") {
                // Clear the hash to prevent drawer issues and open in new tab
                history.replaceState(null, null, window.location.pathname + window.location.search);
                window.open("https://design.amorepacific.com/work/2026/ap-beauty/wordmark/", "_blank");
                return;
            }
            if (currentProjectKey !== key || !drawer.classList.contains("active")) {
                openProjectDrawer(key);
            }
        }
    } else {
        if (drawer.classList.contains("active")) {
            closeProjectDrawerInternal();
        }
    }
}

// Listen for hash changes
window.addEventListener("hashchange", checkHashAndRoute);

// Run router immediately on load
checkHashAndRoute();

// 8. Experience Career Calculator (Start date: July 2012)
function calculateExperience() {
    const startDate = new Date(2012, 6); // July 2012 (0-indexed month)
    const currentDate = new Date();

    let years = currentDate.getFullYear() - startDate.getFullYear();
    let months = currentDate.getMonth() - startDate.getMonth();

    if (months < 0) {
        years--;
        months += 12;
    }

    const expText = `${years} Years ${months} Months`;
    const expEl = document.getElementById("calculated-experience");
    if (expEl) {
        expEl.innerText = expText;
    }
}
calculateExperience();

// 9. Interactive Hero: bg_c.png Rotation respond to mouse
function initHeroInteractions() {
    const heroSection = document.getElementById("hero");
    const introContainer = document.getElementById("intro-container");
    const rotatingCircle = document.getElementById("hero-rotating-circle");

    if (!introContainer || !rotatingCircle) return;

    if (heroSection) {
        heroSection.addEventListener("click", () => {
            const aboutSection = document.getElementById("about");
            if (aboutSection) {
                aboutSection.scrollIntoView({ behavior: "smooth" });
            }
        });
    }

    // Rotate speed and angle variables for rotatingCircle
    let targetAngle = 0;
    let currentAngle = 0;

    // Mouse movement inside Hero Container
    introContainer.addEventListener("mousemove", (e) => {
        const rect = introContainer.getBoundingClientRect();

        // Rotate circle background based on mouse position relative to center
        const centerX = rect.left + rect.width / 2;
        const centerY = rect.top + rect.height / 2;

        // Calculate angle between center and mouse
        const dx = e.clientX - centerX;
        const dy = e.clientY - centerY;

        // Angle in degrees
        targetAngle = Math.atan2(dy, dx) * (180 / Math.PI);
    });

    // Smooth angle interpolation for rotatingCircle
    function animateCircleRotation() {
        if (rotatingCircle) {
            // Lerp angle
            let diff = targetAngle - currentAngle;

            // Normalize diff to -180 to 180 to prevent wrapping spin glitches
            diff = Math.atan2(Math.sin(diff * Math.PI / 180), Math.cos(diff * Math.PI / 180)) * (180 / Math.PI);

            currentAngle += diff * 0.08; // Smooth factor
            rotatingCircle.style.transform = `rotate(${currentAngle}deg)`;
        }
        requestAnimationFrame(animateCircleRotation);
    }
    animateCircleRotation();
}
initHeroInteractions();

// 10. Smart Scroll Header: Keep header fixed, apply color toggle below hero section
function initScrollHeader() {
    const headerEl = document.querySelector(".header");
    const heroSection = document.getElementById("hero");

    if (!headerEl) return;

    window.addEventListener("scroll", () => {
        const currentScrollY = window.scrollY;
        const heroHeight = heroSection ? heroSection.offsetHeight : window.innerHeight;

        // Scrolled-in State: Apply blur filter and change font colors to black below hero
        if (currentScrollY > heroHeight - 80) {
            headerEl.classList.add("header--scrolled");
        } else {
            headerEl.classList.remove("header--scrolled");
        }
    }, { passive: true });
}
initScrollHeader();


